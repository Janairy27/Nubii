El comando para activar el servidor de frontend es: npm start
  - Aunque antes de colocar ese comando se debe de tener en ceunta tener instalado npm, para instalarlo se necesita colocar el comando: npm install
Para activar el servidor de backend es: node app.js
